#> asset:spawner/247/
#
# スポナーの呪われた神器の位置を書く
#
# @within tag/function asset:spawner/register

execute unless data storage asset:spawner DPR[{D:overworld,X:-125,Y:166,Z:-51}] in overworld positioned -125 166 -51 if entity @p[distance=..40] run function asset:spawner/247/register